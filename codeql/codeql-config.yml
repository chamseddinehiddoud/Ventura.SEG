name: "Ventura.SEG Security Configuration"

queries:
  - uses: security-extended
  - uses: security-and-quality
  - uses: ./queries

paths:
  - src
  - tests

paths-ignore:
  - "**/__pycache__/**"
  - "**/.venv/**"
  - "**/venv/**"
